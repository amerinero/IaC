# Mi primer template YAML para automatizar despliegues
#
# Usar "gcloud.cmd config get-value project"  para saber el código de nuestro proyecto
# Usar "gcloud.cmd deployment-manager deployments create my-first-deployment --config vm.yaml" para crear el despliegue.
# ¡¡OJO!! que solo con ese comando ya lanza la máquina
# Usar "gcloud.cmd deployment-manager deployments describe my-first-deployment" para ver propiedades del despliegue.
# Usar "gcloud.cmd deployment-manager deployments delete my-first-deployment" para borrarlo todo
